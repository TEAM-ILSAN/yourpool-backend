from datetime import datetime
import imp
# from apscheduler.apschedulers.background import BackgourndScheuler
# from .jobs import schedule_api, timChecking

# def start():
    # print("이야~")
    # apscheduler = BackgourndScheuler()
    # apscheduler.app_job(schedule_api, 'cron', second=5)
    # apscheduler.app_job(schedule_api, 'interval', second=1)
    # apscheduler.start()